import { CreatePostSkeleton } from './CreatePostSkeleton'

export function UploadStep() {
  return (
    <CreatePostSkeleton
      description="Upload UI boundary only. File input, validation and object URLs come in Dev 2 PR."
      title="Upload"
    />
  )
}
