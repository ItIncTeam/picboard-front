export type SendPasswordRecoveryLinkParams = {
  email: string
}

export const sendPasswordRecoveryLink = async (_params: SendPasswordRecoveryLinkParams) => {
  // TODO: replace with GraphQL mutation when backend provides schema.
}
