apiVersion: v1
kind: Service

metadata:
  name: picboard-frontend-service
  namespace: picboard-space

spec:
  selector:
    project: picboard-frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 4310

  type: ClusterIP
