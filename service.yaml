apiVersion: v1
kind: Service
metadata:
  name: picboard-frontend-service
  namespace: picboard-space
spec:
  type: ClusterIP
  selector:
    project: picboard-frontend
  ports:
    - name: http
      protocol: TCP
      port: 4310
      targetPort: 3000
